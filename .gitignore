# This is a PUBLIC GitHub Pages repo — never commit client documents, PII, or secrets.

# Editor/scratch
whats-next.md
**/whats-next.md

# Client documents (legal / governance / PII) — must NEVER be in a public repo
**/docs/
**/*.pdf
**/*.docx
**/*.doc

# Serverless functions + deps (belong to the client's own hosting, not this public repo)
**/netlify/functions/
**/node_modules/
**/package.json
**/package-lock.json
.netlify/

# Removed from this PUBLIC repo (history-scrubbed 2026-07-11) — keep them out for good.
# command-center = internal agency dashboard (MRR, client + prospect pipeline). NOT for public hosting.
# icare-foundation = stale duplicate; live site is icaregives.org (Netlify, client-owned).
command-center/
icare-foundation/
